<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:allowBackup="false">

        <!-- MainActivity — startuje TtsService i się zamyka -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- SpeakActivity — niewidoczna, odbiera tekst i wywołuje TTS -->
        <!-- Wywoływana przez Kodi: StartAndroidActivity(pl.fanvod.tts, pl.fanvod.tts.SPEAK) -->
        <activity
            android:name=".SpeakActivity"
            android:exported="true"
            android:theme="@android:style/Theme.NoDisplay"
            android:excludeFromRecents="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="pl.fanvod.tts.SPEAK" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- TtsService — inicjalizuje TTS, działa w tle -->
        <service
            android:name=".TtsService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="pl.fanvod.tts.SPEAK" />
            </intent-filter>
        </service>

        <!-- SpeakReceiver — odbiera broadcast intent -->
        <receiver
            android:name=".SpeakReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="pl.fanvod.tts.SPEAK" />
            </intent-filter>
        </receiver>

        <!-- BootReceiver — autostart po włączeniu TV -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
